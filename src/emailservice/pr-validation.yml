trigger: none

pr:
  branches:
    include:
      - develop
      - release/*
      - main

  paths:
    include:
      - src/emailservice/**

jobs:
- template: ../../pipelines/pr-validation-template.yaml
  parameters:
    serviceName: emailservice
