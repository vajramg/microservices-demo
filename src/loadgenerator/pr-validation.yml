trigger: none

pr:
  branches:
    include:
      - develop
      - release/*
      - main

  paths:
    include:
      - src/loadgenerator/**

jobs:
- template: ../../pipelines/pr-validation-template.yaml
  parameters:
    serviceName: loadgenerator
