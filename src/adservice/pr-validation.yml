trigger: none

pr:
  branches:
    include:
      - develop
      - release/*
      - main

  paths:
    include:
      - src/adservice/**

jobs:
- template: ../../pipelines/pr-validation-template.yaml
  parameters:
    serviceName: adservice