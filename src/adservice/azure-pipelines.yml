trigger:
  branches:
    include:
      - src/adservice/**

variables:
  acr: vg1988acr
stages:
  - stage: build
    displayName: Build
    jobs:
      - template: ../../pipelines/build-template.yaml
        parameters:
          servicename: adservice
          acr: $(acr)
  - stage: deploy
    displayName: Deploy
    jobs:
      - template: ../../pipeline/deploy-template.yaml
        parameters:
          servicename: adservice
          acr: $(acr)
