trigger: none

pr:
  branches:
    include:
      - develop
      - release/*
      - main

  paths:
    include:
      - src/frontend/**

jobs:
- template: ../../pipelines/pr-validation-template.yaml
  parameters:
    serviceName: frontend
