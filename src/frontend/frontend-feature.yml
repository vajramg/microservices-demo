trigger:
  branches:
    include:
      - feature/*
  paths:
    include:
      - src/frontend/**

pr: none

jobs:
- template: ../../pipelines/feature-ci-template.yaml
  parameters:
    serviceName: frontend
