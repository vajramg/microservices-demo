trigger:
  branches:
    include:
      - feature/*
  paths:
    include:
      - src/checkoutservice/**

pr: none

jobs:
- template: ../../pipelines/feature-ci-template.yaml
  parameters:
    serviceName: checkoutservice
