trigger:
  branches:
    include:
      - feature/*
  paths:
    include:
      - src/adservice/**

pr: none

jobs:
- template: ../../pipelines/feature-ci-template.yaml
  parameters:
    serviceName: adservice