apiVersion: v1
kind: Service
metadata:
  name: currency-service
  labels:
    app: currency
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: currency
  ports:
    - port: 7000
      targetPort: 7000
      
    