trigger:
  branches:
    include:
      - feature/*
  paths:
    include:
      - src/currencyservice/**

pr: none

jobs:
- template: ../../pipelines/feature-ci-template.yaml
  parameters:
    serviceName: currencyservice
