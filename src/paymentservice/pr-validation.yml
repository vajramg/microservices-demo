trigger: none

pr:
  branches:
    include:
      - develop
      - release/*
      - main

  paths:
    include:
      - src/paymentservice/**

jobs:
- template: ../../pipelines/pr-validation-template.yaml
  parameters:
    serviceName: paymentservice
