trigger:
  branches:
    include:
      - feature/*
  paths:
    include:
      - src/paymentservice/**

pr: none

jobs:
- template: ../../pipelines/feature-ci-template.yaml
  parameters:
    serviceName: paymentservice
